# To-Do
- toggel y-scale between linear and log on a per pannel basis
- if new pannels are added in log mode they don't display any data. Only after toggeling the log mode off and on again. This is a bug that needs to be fixed.
- if a plot is diatached and then a new one added it 