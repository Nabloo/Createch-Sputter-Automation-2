# To-Do
- safe all devices into one csv file. Make sure the loading works correctly.
- sqm data can be loaded from a file
- name the Plot widget after the device and update the name on device change
- have gaps in the plots when datapoints are to far apart also in live mode, just like in the log mode
- clicking one channel in the channel selecting dialog selects all channels instead of just clicking one

## Logs
- reload all plots after loading a new log file. 
- If there is no data for an open plot, then display "no data" 
- weird line on change between live and log mode in the live mode from the first data point to the last
- changing the loaded log file in the log mode does not work