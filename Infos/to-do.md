# To-Do
- load previous data from that day to live mode
- multiple plots next to each other

## Architecture
- unify _update_x_axis_label and _apply_x_axis_label
  - actually set_axis_mode and _apply_axis_mode are identical lol